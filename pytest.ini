[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Fast unit tests (no network I/O)
    integration: Tests that require live Elasticsearch/Kibana
    slow: Tests that may take > 5 seconds
